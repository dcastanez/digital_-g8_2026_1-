module BCD_BIN(
                input clk,
                input init,
                input rst,
                input [15:0] BCD,
                output [15:0] BIN
                );

                wire [15:0] BIN;
endmodule
